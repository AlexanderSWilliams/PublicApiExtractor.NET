# PublicApiExtractor.NET
Public API Extractor is a small .NET command-line tool that outputs the specification of the public API surface of any managed portable executable file with CLI metadata, such as a managed .dll, managed .exe, reference assembly, facade assembly, or compatible metadata-only PE. Native-only PE files are rejected.
